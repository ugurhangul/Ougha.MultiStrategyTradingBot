If symbol is not in trading session, sleep it thread until active trading session
For breakout strategies, if reference candle is not available, sleep thread until it is available
Make decorator for validation methods to fetch them and validate them automatically
